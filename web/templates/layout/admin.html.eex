<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Timetable</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/reset.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/semantic.min.css") %>">
    <!--<link rel="stylesheet" href="<%= static_path(@conn, "/css/icon.css") %>">-->
    <!--<link rel="stylesheet" href="<%= static_path(@conn, "/css/timetable.css") %>">-->
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/changa.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/roboto.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/style.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/accordion.css") %>">

  </head>

<body>
<!-- Page Contents -->
<div class="pusher">

  <div class="ui inverted vertical header segment">
    <div class="ui container">
      <div class="ui stackable inverted divided equal height stackable grid center aligned">
        <h1>[Timetable]</h1>
      </div>
    </div>
  </div>

  <main role="main">
      <div class="ui container">
        <%= render @view_module, @view_template, assigns %>
      </div>
  </main>

  <div class="ui inverted vertical footer segment">
    <div class="ui container">
      <div class="ui stackable inverted divided equal height stackable grid">
        <div class="three wide column">
          <h4 class="ui inverted header">Links</h4>
          <div class="ui inverted link list">
            <a href="/" class="item">Home</a>
          </div>
          <div class="ui inverted link list">
            <%= link "Admin", to: prayer_path(@conn, :index), method: :get, class: "item" %>
          </div>
        </div>
        <div class="seven wide column">
          <h4 class="ui inverted header">About</h4>
          <p>Timetable for Jamaa' Prayers</p>
          <%= link to: prayer_path(@conn, :reboot), method: :get, data: [confirm: "Are you sure?"], class: "ui negative" do %>
              <i class="refresh icon"></i> Reboot
          <% end %>
          <br />
          <%= link to: prayer_path(@conn, :shutdown), method: :get, data: [confirm: "Are you sure?"], class: "ui negative" do %>
              <i class="refresh icon"></i> Shutdown
          <% end %>
          <br />
          <%= link to: prayer_path(@conn, :github), method: :get, data: [confirm: "Are you sure?"], class: "ui negative" do %>
              <i class="refresh icon"></i> Update
          <% end %>
        </div>
      </div>
    </div>
  </div>

</div> <!-- /pusher -->

    <script src="<%= static_path(@conn, "/js/jquery.slim.min.js") %>"></script>

    <script src="<%= static_path(@conn, "/js/db/timetable.js") %>"></script>

</body>
</html>
